# fluttered_api

API server for Fluttered projects

## Development

1. Clone this project.
2. Run `composer install` on the project's root.
3. Rename `.env.example` to `.env`.
4. Run `php artisan key:generate`.
5. Run `php artisan serve` to start application.
6. Open to http://localhost:8000 in your browser.
